# Use root/example as user/password credentials
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: Kueens-b
      MONGO_INITDB_ROOT_PASSWORD: Kueens111
  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 3000:3000
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: K13032022
      ME_CONFIG_MONGODB_URL: mongodb://root:example@mongo:27017/    